/**
 * config
 * Module for storing
 */

export { remoteUrls, remotePrefix };

const remotePrefix = "/ajax";
const remoteUrls = {
    events: {
        calendar: '/calendar',
        list: '/events'
    }
}
